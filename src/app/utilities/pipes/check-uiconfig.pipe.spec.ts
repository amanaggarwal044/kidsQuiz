import { CheckUIConfigPipe } from './check-uiconfig.pipe';

describe('CheckUIConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckUIConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
