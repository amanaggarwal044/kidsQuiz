import { AddBlockClassPipe } from './add-block-class.pipe';

describe('AddBlockClassPipe', () => {
  it('create an instance', () => {
    const pipe = new AddBlockClassPipe();
    expect(pipe).toBeTruthy();
  });
});
